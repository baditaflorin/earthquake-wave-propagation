# Privacy

This Mode A app ships with no analytics and no backend.

The browser may store non-sensitive local preferences such as sound gain,
attenuation, and visualization choices in `localStorage`. These values stay on
the user's device and can be removed by clearing site data.

# Contributing

Thanks for helping make earthquake-wave propagation easier to explore.

## Local setup

1. Install Node.js 22 or newer.
2. Run `npm install`.
3. Run `make install-hooks`.
4. Run `make dev`.

Use Conventional Commits for commit messages, for example `feat: add wave controls`.
